-module(fpl2_4).
-export([task/1]).

% Порахувати кількість елементів списку:
task([]) -> 
    % Якщо довжина порожнього списку 0
    0;
task([_ | T]) -> 
    % Рекурсивний обхід списку і збільшення лічильника
    1 + task(T).