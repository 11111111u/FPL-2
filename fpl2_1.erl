-module(fpl2_1).
-export([task/1]).

% Знайти останній елемент списку:
task([H]) -> 
    % Якщо список містить лише один елемент - це останній елемент
    H;
  task([_ | T]) -> 
    % Рекурсивно обходимо список
    task(T);
  task([]) -> 
    error(empty_list).